# Ревью модуля формы «Запрос на партнерский договор»

Файл: `ЗапросНаПартнерскийДоговор.txt` (модуль управляемой формы, 5 577 строк).
Номера строк ниже — по этому файлу.

## Вердикт

Модуль рабочий «на удачном сценарии», но это классический говнокод: вся бизнес-логика
(создание контрагентов, физлиц, адресов, счетов, договоров, печать) зашита в модуль
формы, продублирована по 2–4 раза, ошибки массово глотаются пустыми `Попытка…КонецПопытки`,
а часть кода содержит реальные баги, которые уже сейчас создают дубли и теряют данные.
Ниже — сначала баги, потом архитектура/производительность/безопасность/стиль.

---

## 1. Реальные баги (ломают данные или падают)

### 1.1. Извлечение номера дома не работает вовсе (строки 121–130, 200–208)
```bsl
Если СтрДлина(СтрЗаменить(МассивСлов[Инд], "0123456789", "")) < СтрДлина(МассивСлов[Инд]) Тогда
```
`СтрЗаменить` ищет **целую подстроку** `"0123456789"`, а не каждую цифру. Условие
сработает только если слово буквально содержит «0123456789». В итоге `Объект.Поле7`
(дом) почти всегда остаётся пустым → `GET_ПохожийАдресВБазе` не ищется → на каждом
нажатии «Заполнить реквизиты» создаётся **новый дубль адреса** (`CREATE_SET_АдресИП`).

### 1.2. Проверка пола — мёртвый код (строки 676–681)
```bsl
Если ЗначениеЗаполнено(Объект.Пол) И Строка(Объект.Пол) = "Физическое лицо" Тогда
    Если НЕ ЗначениеЗаполнено(Объект.Пол) Тогда   // недостижимо
```
`Пол` сравнивается со строкой «Физическое лицо» (это значение реквизита ЮрФизЛицо,
а не пола) — внешнее условие никогда не истинно, вложенное противоречит внешнему.
Пол самозанятого фактически не проверяется.

### 1.3. `ПередЗаписью` не ставит `Отказ` (строки 3840–3854)
При незаполненном «Ответственном» выводится сообщение, но `Отказ = Истина` не
устанавливается — документ записывается всё равно. Валидации нет, есть её имитация.

### 1.4. Обращения к методам у `Неопределено`
- строка 2153–2154: `тСчет = GET_БанковскийСчетИзБазы(тИНН); SET_БанкВОбъектИзБазы(тСчет.Банк.Код);`
  — если счёта в базе нет, `тСчет = Неопределено` → падение. Вызывается с клиента
  при **каждом изменении текста ИНН** (`ИННИзменениеТекстаРедактирования` → `SET_ОбъектДляФИЗЛИЦА`).
- строка 2351: `Не нКонтрагент.Пустая()` — а `GET_КонтрагентаВБазеПоИНН` возвращает
  `Неопределено`, у которого нет метода `Пустая()`.
- строка 939: `GET_КонтрагентаВБазеПоИНН(Объект.ИНН).ПолучитьОбъект()` — без проверки
  на `Неопределено`.
- строка 2275: `нКонтрагентОбъект = нКонтрагент.ПолучитьОбъект()` — то же самое.

### 1.5. Реквизиты существующего счёта не заполняются (строки 2345–2363)
```bsl
Если СсылкаСчета <> Неопределено Тогда
    SET_РеквизитыДокументаИзБанковскогоСчета(тСчет);   // тСчет, а не СсылкаСчета
```
Когда счёт найден и номер совпал, `тСчет` не присваивался — в функцию уходит
`Неопределено`, и она молча выходит. Реквизиты документа из существующего счёта
не подтягиваются.

### 1.6. `Записать()` формы внутри серверной функции (строка 1167)
`СОЗДАТЬ_ПолучитьДоговорНаСервере` — `&НаСервере`, но вызывает `Записать()`
(метод управляемой формы, доступный только на клиенте). Это либо падает и
маскируется внешней `Попыткой` с откатом транзакции, либо работает случайно.
Показателен и тестовый пример из Экселя: `ДоговорКонтрагента = <Пустая ссылка>`.

### 1.7. Договор записывается трижды, имя формируется до присвоения номера
- строка 1110: `нДоговор.Наименование = … + Строка(нДоговор.НомерДоговора) + …` —
  номер ещё не установлен;
- записи на строках 1112, 1143, затем перечитывание через `НайтиПоКоду` (1145) и
  ещё одна запись 1154. Три записи одного элемента в одной транзакции, между ними —
  запросы. Лишние блокировки и версии.
- строки 1021–1026: одна и та же проверка `НЕ ЗначениеЗаполнено(нКонтрагентСсылка)`
  дважды подряд добавляет два разных сообщения.

### 1.8. Паспортные данные: пишем «как ввели», ищем нормализованное (1406–1512)
Поиск в регистре идёт по серии, очищенной от нецифр (`Серия1`/`Серия2`), а
`CREATE_ПаспортныеДанныеФизЛица` записывает `Объект.ДокументСерия` без нормализации.
Одни и те же паспортные данные в разном написании создадут дубли записей регистра.

### 1.9. Дублирование зданий в адресе из ФИАС (строки 407–408, 556–557)
```bsl
Здание = ТРег(…Здание[0]…Номер)+" "+НРег(…Здание[1]…Тип)+" "+ТРег(…Здание[0]…Номер);
```
Второй номер берётся снова из `Здание[0]` — получится «5 корп 5» вместо «5 корп 2».
Плюс инициализируется переменная `тЗдания` (строки 328, 494), а используется `тЗдание` —
если все `Попытки` упали, в `CREATE_SET_ЮридическийАдресПоКлассификатору` уходит
`Неопределено`, конкатенация со строкой падает, и вся транзакция откатывается с
невнятным сообщением.

### 1.10. Мёртвый lookup региона (строки 105–116, 185–196)
`Регион = GET_РегионПоИндексу(Число(Индекс))` вызывается, когда локальная `Индекс`
ещё пустая строка (`Число("") = 0`), результат глушится пустой `Попыткой`, а ниже тот же
lookup повторяется уже по `Объект.Индекс`. Первый блок — мусор, который просто ест
время и маскирует ошибки.

### 1.11. `&НаСервере Перем` не живут между серверными вызовами (строки 3–14)
`нКодКонтрагента`, `нФИЗЛИЦО_Ссылка`, `нЮРЛИЦО_Ссылка` — серверные переменные модуля
формы, они обнуляются между серверными вызовами. `СохранитьФайлВСправочник` (5587)
и `CREATE_SET_ЮридическийАдресПоКлассификатору` (2922+) полагаются на то, что они
заполнены «когда-то раньше» — при вызове из другой кнопки/сеанса получат `Неопределено`
и молча уйдут в другую ветку (не тот владелец адреса/файла).

### 1.12. Определение типа лица по `ВыделенныйТекст` (строки 3575, 3645, 3792, 3919…)
`Элементы.ЮрФизЛицо.ВыделенныйТекст` — это выделенный текст в поле, а не выбранное
значение. При открытии формы выделения обычно нет → интерфейс сваливается в ветку
«Прочее». Правильно сравнивать `Объект.ЮрФизЛицо` с перечислением (в других местах
так и делается — стилей два одновременно).

### 1.13. `ОписаниеОшибки()` вне обработчика исключения (строки 995–997)
В `СоздатьДоговор` предупреждение показывает `ОписаниеОшибки()` вне `Исключение` —
там будет пусто или мусор от прошлой ошибки.

### 1.14. Недостижимый код в проверках КИ (строки 2479–2491, 2528–2537)
```bsl
Если НЕ ЗначениеЗаполнено(Объект.Телефон) Тогда
    тМассивНезаполненныхРеквизитов.Добавить("Телефон");
    Возврат Неопределено;          // выходим сразу
КонецЕсли;
Если тМассивНезаполненныхРеквизитов.Количество() > 0 Тогда  // недостижимо
    ВызватьИсключение Сообщение;
```
Вся ветка с исключением — мёртвая. Аналогично в Email-версии.

### 1.15. `ПередЗаписьюНаСервере()` никогда не вызывается (строки 3877–3879)
Сигнатура не совпадает с обработчиком платформы (`ПередЗаписьюНаСервере(Отказ,
ТекущийОбъект, ПараметрыЗаписи)`), из кода функция тоже не вызывается. Очистка
`Объект.Комментарий` не происходит.

### 1.16. Присваивания, о которых автор знает, что они не работают (строки 940–941)
```bsl
нКонтрагентОбъект.Телефоны = Объект.ТелефонКИФизЛица; // Недоступно для записи
нКонтрагентОбъект.Email = Объект.Email;               // Недоступно для записи
```
Плюс строкой ниже безусловно `ПлательщикНДС = Истина` — затирает значение,
которое чуть раньше аккуратно выставлялось из `Объект.ПлательщикНДС`.

---

## 2. Опасные практики

### 2.1. Внешние вызовы и интерактив внутри транзакций
Внутри `НачатьТранзакцию()` выполняются HTTP-запросы к ЕГРЮЛ (`ЕГРЮЛ.АйтиСофт`,
строки 99–103, 178–182, 308–312, 474–478) и `Сообщить(...)`. Долгий/висящий внешний
сервис держит блокировки БД. Исключения от ЕГРЮЛ глотаются, и код идёт дальше с
неинициализированной `СтруктураОтвета` — следующее обращение к ней падает уже
внутри транзакции.

### 2.2. Склейка ФИО в командную строку + запуск ruby.exe (2836–2861, 5455–5471)
```bsl
СтрокаЗапроса = "C:\Ruby34-x64\bin\ruby.exe C:\proj\morfer2.rb "+Фамилия+" "+Имя+" "+Отчество;
WshShell = Новый COMОбъект("WScript.Shell");
WshShell.Run(СтрокаЗапроса, 0, -1);
```
- пользовательский ввод без экранирования уходит в командную строку — инъекция команд;
- захардкожены пути, причём в двух местах **разные версии Ruby** (Ruby34 и Ruby27);
- обмен через один общий файл `C:\proj\morfer.txt` — два одновременных пользователя
  затирают результаты друг друга (race condition), файл читается без проверки, что
  ruby вообще отработал;
- это выполняется на **сервере** при каждом окончании ввода Фамилии/Имени/Отчества/Пола.
Склонение ФИО надо вынести в общий модуль с нормальным вызовом сервиса (HTTP/COM)
или локальной библиотекой, без файлов в `C:\proj`.

### 2.3. Пустые обработчики исключений — десятки
`Исключение КонецПопытки;` по всему модулю (адресный разбор, запись справочников,
`SET_РеквизитыДокументаИзБанковскогоСчета` и т.д.). Ошибки записи данных исчезают
бесследно; отлаживать такие «иногда не заполнилось» потом невозможно. Минимум —
писать в журнал регистрации.

### 2.4. Word COM без освобождения при ошибке (4297–4384, 4683–4778)
Если упадёт `Documents.Open` или блок замен — `Word.Quit()` не вызывается, на клиенте
копятся зомби-процессы WINWORD.EXE. Нужен `Попытка…Исключение` с гарантированным Quit.

### 2.5. Персональные данные
Паспортные данные и сканы сохраняются на диск по пути из константы, имя файла
включает ФИО (`"ПаспортФЛ" + Наименование + ...`, строки 1451, 1506). Ни контроля
доступа, ни шифрования — стоит хотя бы осознанно зафиксировать это решение.

---

## 3. Копипаста (главная болезнь модуля)

- Ветки ИП/ЮЛ и «новый/существующий контрагент» в `ЗАПОЛНИТЬ_РеквизитыНаСервере` —
  четыре почти идентичных блока разбора ответа ЕГРЮЛ по ~100–150 строк
  (97–162 ≈ 175–239; 305–459 ≈ 471–607). Исправление бага 1.1 придётся вносить в
  четыре места.
- Блок «обновить ПлательщикНДС + доп.реквизит Агентский НДС» скопирован трижды
  (251–279, 621–649, 879–907).
- `GET_SET_КИТелефонФизЛица` и `GET_SET_КИEmailФизЛица` — два одинаковых запроса
  на ~25 полей каждый (используется 5–7).
- `ЗаполнитьШаблонИСохранитьОдинФормат` и `ЗаполнитьШаблонИСохранитьНаКлиенте` —
  две копии одной процедуры работы с Word (~90 строк каждая).
- Управление видимостью в `ПриОткрытии` / `ЮрФизЛицоПриИзменении` — ~600 строк
  присваиваний `Видимость/ОтметкаНезаполненного` тремя копиями на ветку. Просится
  одна процедура `УстановитьРежимФормы(Режим)` с таблицей настроек.
- Email-валидация продублирована в двух обработчиках (4188–4209), и оба слабые:
  `a@b.` проходит.

---

## 4. Производительность

- **Серверные вызовы и ЕГРЮЛ на каждый ввод текста**: `ИННИзменениеТекстаРедактирования`
  делает до 3 серверных вызовов + внешний запрос ЕГРЮЛ; окончание ввода Фамилии/Имени/
  Отчества/Пола каждый раз запускает ruby.exe на сервере.
- **Запросы в циклах**: `НайтиПоНаименованию("Агентский НДС")` вызывается внутри
  цикла по доп.реквизитам (259–265 и копии) — запрос на каждую итерацию.
- **Обращения к реквизитам через точку от ссылок** в циклах (`Выборка.Ссылка.Тип`,
  `Выборка.Ссылка.Фамилия`, строки 845–853, 1815, 1845…) — неявный запрос на каждое
  обращение. Поля надо включать в исходный запрос.
- Условия вида `Владелец.ИНН = &ИНН` по составному типу владельца без `ВЫРАЗИТЬ` —
  соединения со всеми таблицами составного типа.
- `SELECT` на 40 полей ради 15 используемых (`GET_SET_АдресФизЛицаИлиИП`, 1668–1720;
  там же дважды условие `ПометкаУдаления = ЛОЖЬ`).
- `GET_ДействующийОткрытыйДоговор` вызывается и на клиенте, и повторно на сервере
  в той же операции (979 и 1012).

---

## 5. Магические значения и модель данных

- Захардкожено: ИНН своей организации `"7802193688"` (1069), подразделение и город
  «Санкт-Петербург» (1070, 2437–2438), тип договора «Агентский договор (диллерский)»
  (1050, 5393 — с орфографической ошибкой, «дилерский»), группы «Дилеры»,
  «Действующие клиенты», «РАЗОБРАТЬ!», свойство «Агентский НДС» — всё через
  `НайтиПоНаименованию`. Переименование любого элемента тихо ломает логику
  (`НайтиПоНаименованию` вернёт пустую ссылку — и часто это никак не проверяется).
- Всем новым контрагентам принудительно ставится Город = Санкт-Петербург (2437) —
  даже региональным.
- Поля адреса называются `Поле2…Поле9` — расшифровка («Поле7 — дом») существует
  только в комментариях и в Эксель-табличке. Комментарий у Поле9 при этом врёт
  («корпус», по факту — квартира, строки 5298–5299).
- `ЗаписьВключена()` всегда возвращает `Истина` (16–19) — мёртвый флаг, которым
  обёрнута примерно половина `Записать()`, вторая половина пишет напрямую
  (например 2367–2368) — непоследовательно.
- `GET_ДействующийОткрытыйДоговор` возвращает то `""`, то `Строка(Ссылка)`, и
  заодно `Сообщить` как побочный эффект (5390–5422). GET-функция не должна
  разговаривать с пользователем.
- `SET_ОбъектДаннымиКонтрагента` возвращает `Строка(нКонтрагент)` — строковое
  представление ссылки, которое никто не использует (2739).

---

## 6. Архитектура и стиль

- **Вся бизнес-логика — в модуле формы.** Создание контрагентов, физлиц, КИ, адресов,
  счетов, договоров, печать, работа с ФИАС — всё это должно жить в общих серверных
  модулях (и переиспользоваться из других форм/обработок), форма — только UI.
- Именование: смесь `GET_/SET_/CREATE_` капсом с русским хвостом; апофеоз —
  `GET_SET_or_CREATE_GET_SET_КонтактноеЛицо`. Имя функции не должно перечислять
  все её побочные эффекты — оно должно их не иметь.
- Комментарии-маркеры `// ИТОГ`, `// +- ИТОГ`, `// НЕ ТРОГАТЬ`,
  `// Всё, что ниже работает, не трогать!` (3532), `// ХЗ??` (3818, 4031) —
  честная самодиагностика: код не понимают даже авторы.
- Сотни строк закомментированного кода (`//Объект.Комментарий = …` повторяется
  десятки раз) — это должно жить в истории конфигурации, а не в модуле.
- В журнал регистрации пишутся комментарии со скобкой-смайликом: `"Успешно)"`,
  `"Ошибка)"` (659, 666, 727, 732).
- Обрезка телефона `Сред(тТелефон, 2)` при длине 11 (1906–1910, 2713–2717) —
  молча теряется код страны, «7…» и «8…» дают разные результаты.
- Нет проверки контрольного числа ИНН — только длина.

---

## 7. Что делать (по приоритету)

1. **Починить баги раздела 1** — прежде всего 1.1 (дубли адресов), 1.3 (валидация),
   1.5 (счёт), 1.6 (запись договора), 1.11 (серверные переменные).
2. **Убрать внешние вызовы (ЕГРЮЛ, ruby) из транзакций**, а склонение ФИО — из
   `C:\proj\morfer.txt` в нормальный сервис.
3. **Вынести бизнес-логику в общий модуль** и свести четыре копии разбора ЕГРЮЛ
   к одной функции `ЗаполнитьАдресИзОтветаЕГРЮЛ(СтруктураОтвета)`.
4. **Заменить пустые Попытки** на минимум `ЗаписьЖР + осмысленное сообщение`, а
   там, где ошибка критична, — дать ей упасть.
5. Заменить `НайтиПоНаименованию`-магию на предопределённые элементы / константы /
   функциональные опции.
6. Свести управление видимостью формы к одной табличной процедуре.
